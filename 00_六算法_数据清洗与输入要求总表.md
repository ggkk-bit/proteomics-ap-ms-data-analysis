# 六个算法的数据清洗要求与输入要求总表

## 一、所有算法共用的基础清洗规则

从 `proteinGroups.txt` 出发时，六个算法都先执行同一层基础过滤：

1. 删除 `Reverse` / 反向匹配。
2. 删除 `Potential contaminant` / 潜在污染物。
3. 删除 `Only identified by site` / 仅位点鉴定蛋白。
4. 删除没有稳定蛋白标识的条目，优先保留 `Majority protein IDs`、`Protein IDs`、`Gene names`。
5. 删除在所有保留样本中都为 0 或缺失的条目。
6. 样本名必须先和样本注释表对应好，再进入算法专属转换。

这一步的依据是 MaxQuant 常规后处理、AP-MS/互作组学通用流程，以及各算法都不直接接受含反向匹配、污染物和仅位点蛋白的原始表。

## 二、各算法的输入要求

### 1. CompPASS

定位：主算法，做 bait-prey 互作打分。

核心输入：长表，每一行是一条 `bait-prey-run` 观测。

最小字段：
- `idRun`
- `idBait`
- `idPrey`
- `countPrey`

推荐附加字段：
- `preyGene`
- `countType`
- `sourceColumn`

当前脚本：
- `01_CompPASS_互作评分/脚本/01_数据清洗_CompPASS.py`
- `01_CompPASS_互作评分/脚本/02_格式转换_CompPASS.py`
- `01_CompPASS_互作评分/脚本/03_确认CompPASS输入文件.py`

### 2. MiST

定位：主算法，用 abundance、reproducibility、specificity 三个维度综合打分。

核心输入：
- `data.txt`
- `keys.txt`
- 可选：`mist.yml`

`data.txt` 推荐字段：
- `id`
- `ms_uniprot_ac`
- `ms_num_unique_peptide`
- `ms_protein_mw`
- 可选的丰度或排序字段

`keys.txt` 最小字段：
- `id`
- `bait_name`

当前脚本：
- `02_MiST_互作评分/脚本/01_数据清洗脚本.R`
- `02_MiST_互作评分/脚本/02_格式转换脚本.R`

### 3. HGSCore

定位：辅助算法，偏向基于谱数/丰度和蛋白长度的辅助评分。

核心输入：
- 样本级长表
- run × prey 原始矩阵
- 长度归一化矩阵
- 蛋白长度表

推荐字段：
- `sample_id`
- `bait_name`
- `prey_id`
- `count_or_intensity`
- `protein_length`

当前脚本：
- `03_HGSCore_辅助评分/脚本/01_输入构建/数据清洗.py`
- `03_HGSCore_辅助评分/脚本/01_输入构建/格式转换.py`
- `03_HGSCore_辅助评分/脚本/01_输入构建/输入文件确认.py`

### 4. CS Score

定位：辅助算法，偏向共现/成员关系与候选配对评分。

核心输入：
- 成员长表
- 二值矩阵
- 候选配对表

推荐字段：
- `complex_id` 或 bait/组别信息
- `prey_id`
- `presence`
- `sample_id`

当前脚本：
- `04_CS_Score_辅助评分/脚本/01_数据清洗/CS_Score_数据清洗.py`
- `04_CS_Score_辅助评分/脚本/02_格式转换/CS_Score_格式转换.py`
- `04_CS_Score_辅助评分/脚本/03_输入文件确认/CS_Score_输入文件确认.py`

### 5. CRAPome

定位：过滤层，用公开背景污染频率帮助排除常见非特异蛋白。

核心输入：上传长表。

最小字段：
- `Bait Name`
- `AP Name`
- `Prey Name`
- `Spectral Count`

注意：
- `proteinGroups.txt` 一般只能提供蛋白过滤和映射，不能独自恢复完整的 `Spectral Count` 上传表。
- 通常还需要 `evidence.txt`、样本注释和样本级计数表。

当前脚本：
- `05_CRAPome_污染过滤/脚本/01_输入文件确认与说明.py`
- `05_CRAPome_污染过滤/脚本/02_数据清洗.py`
- `05_CRAPome_污染过滤/脚本/03_格式转换_CRAPome输入.py`

### 6. PPIrank

定位：补充排序层，在 bait-prey 基础上结合对照、重复和网络信息再排序。

核心输入：长表。

最小字段：
- `bait_name`
- `replicate_id`
- `prey_id`
- `spectral_count`
- `is_control`

推荐附加字段：
- `sample_id`
- `condition`
- `gene_symbol`
- `protein_length`

当前脚本：
- `06_PPIrank_网络补充排序/脚本/01_数据清洗脚本.py`
- `06_PPIrank_网络补充排序/脚本/02_格式转换脚本.py`
- `06_PPIrank_网络补充排序/脚本/03_输入文件确认脚本.py`

## 三、现阶段脚本状态

现在六个算法都已经开始有各自独立的清洗和格式转换脚本，但真实可直接跑通程度并不完全一样：

- `CompPASS`、`MiST`：已经有较完整的清洗 + 转换入口。
- `HGSCore`、`CS Score`：适合先做辅助评分测试，后续还可以继续细化真实评分环节。
- `CRAPome`、`PPIrank`：最依赖真实样本级谱数/计数输入，目前脚本已经把格式骨架搭好，但还需要你后续提供真实计数来源才能完全落地。

# 结果筛选说明

默认筛选规则：
- 分数取前 5% 左右，也就是 `quantile = 0.95`
- 至少 `2` 个重复或支持证据
- 如果存在 CRAPome 频率列，默认 `<= 0.1`

为什么这样做：
- HGSCore 的绝对分数阈值通常不是跨项目统一的
- 用分位数可以适应不同数据规模和打分尺度
- 重复支持可以减少单次随机富集带来的假阳性
- CRAPome 频率可以进一步压低常见背景蛋白

不确定点：
- 这些阈值是本项目的默认工程参数，不是所有 HGSCore 文献都通用
- 如果你后续有验证集，建议改成按 ROC/PR 或已知复合体命中率再调

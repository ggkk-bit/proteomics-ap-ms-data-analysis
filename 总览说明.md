## 多算法分析工作区

这个工作区围绕 `proteinGroups.txt` 组织，目标不是统一复制同一套目录，而是:

1. 让每个算法都保留自己的输入习惯和结果风格。
2. 保持中文目录、中文脚本名，便于长期维护。
3. 让你后续替换数据时，仍然能沿着固定流程继续分析。

当前六个算法分为四类:

- 主评分: `CompPASS`、`MiST`
- 辅助评分: `HGSCore`、`CS Score`
- 过滤层: `CRAPome`
- 补充排序: `PPIrank`

统一原则:

- `1.原始输入` 只放原始表，不在这里改数据
- `2.样本信息与分组` 或 `2.对照与背景定义` 负责样本注释
- `3.数据清洗` 负责去除 `Reverse`、`Potential contaminant`、`Only identified by site`
- 中间输入文件单独放，避免覆盖
- 结果、可视化、参考说明分开放
- 每个任务一个脚本，脚本名称直接说明用途

模板输入数据:

- `C:\Users\dakey\Desktop\01_进行中\古老蛋白算法调研\下载的文献进行测试\proteinGroups.txt`

说明:

- 现阶段先建立骨架和说明文件
- 下一步再往每个算法目录里补真实清洗脚本、格式转换脚本、结果筛选说明和可视化脚本
